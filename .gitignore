node_modules/
.env
.DS_Store
*.log

# PM2
logs/
.pm2/

# User-specific state
atlas.positions.json

# SQLite database (runtime state — devices, users, audit log)
atlas.db

# Deployment config (instance-specific — copy from atlas.config.example.json)
atlas.config.json

# Auth runtime files (now in atlas.db, kept for legacy)
users.json
audit-log.json
.jwt-secret

# FRR proto files (copy from your local FRR installation)
src/proto/*.proto
!src/proto/gnmi.proto

# Stale test/capture data
command_output.json
tunnel_fib_live.json
