# ATLAS Configuration
PORT=3000

# Default device credentials (can be overridden per-device)
DEFAULT_DEVICE_USERNAME=admin
DEFAULT_DEVICE_PASSWORD=admin

# Session secret for future auth
SESSION_SECRET=change-me-to-a-random-string
